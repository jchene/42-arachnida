{
  "scripts": {
    "build": "npx tsc",
    "run":"node dist/spider/index.js",
    "start": "npm run build && npm run run",
    "clean": "rm -rf dist/*.svg dist/*.png dist/*.jpg dist/*.jpeg dist/*.gif"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "cheerio": "^1.0.0-rc.12",
    "commander": "^11.0.0",
    "typescript": "^5.1.6",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/cheerio": "^0.22.31",
    "@types/node": "^20.4.5",
    "@types/uuid": "^9.0.2"
  }
}
